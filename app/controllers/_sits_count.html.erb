  <% arr = @train.wagons.where(types:str) %>
  <%= coupe = arr.size %>
  <strong> || верхних полок : </strong>
  <%= arr.inject(0) {|acc, iter| acc += iter.upper }%>
   <strong> нижних полок : </strong>
  <%= arr.inject(0) {|acc, iter| acc += iter.lower }%><br>