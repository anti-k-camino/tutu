<p>
  <strong> <%= t('.name') %></strong>
  <%= @user.name %><br>
  <strong> Email :</strong>
  <%= @user.email %>
</p>
<!--<p> Билеты у пользователя :</p>
<% if @user.tickets.empty? %>
  <p>У пользователя нету билетов</p>
<% else %>
  <ul>
   <% @user.tickets.each do |ticket| %>   
    <li><%= link_to "#{ticket.number}", ticket_path(ticket) %></li>
   <% end %>
  </ul>
<% end %>-->
<%=link_to t('.tickets'), tickets_path %><br>

<%= link_to t('common.main'), root_path %> |
<%= link_to t('common.edit'), edit_user_registration_path(@user) %> 
