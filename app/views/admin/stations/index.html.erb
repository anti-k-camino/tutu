<h1><%= t('stations.all.title')%></h1>

<table>
  <thead>
    <tr>
      <th><%= t('stations.all.name') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @stations %>  
  </tbody>
</table>

<br>

<%= link_to t('stations.new.link'), new_admin_station_path %>
<%= link_to t('common.main'), root_path %>
