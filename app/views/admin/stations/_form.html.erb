<%= form_for([:admin, @station]) do |f| %>
  <%= render 'common/errors', resource: @station %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit t('.submit') %>
  </div>
<% end %>
