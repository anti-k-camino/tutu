
<div class="container">    
  <div calss="row" style="margin-top:50px;">
    <div class="col-xs-12 col-sm-6">
      <%= bootstrap_form_for([:admin, @user]) do |f| %>  
        <div class="field">    
          <%= f.text_field :name %>
        </div>
        <div class="actions">
          <%= f.submit 'ИЗМЕНИТЬ' %>
        </div>
      <% end %>
    </div>
  </div>
</div>