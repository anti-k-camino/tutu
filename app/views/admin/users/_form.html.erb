<%= form_for([:admin, @user]) do |f| %>
  <%= render 'common/errors', resource: @user%>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'ИЗМЕНИТЬ' %>
  </div>
<% end %>