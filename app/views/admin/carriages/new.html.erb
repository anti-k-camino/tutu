<h1><%= t('.title')%></h1>
<%= render 'form' %>
<div class='btn-group'>
  <%= link_to t('common.main'), root_path, class:'btn' %>
  <%= link_to t('common.back'), admin_train_path(params["train_id"]), class:'btn'%>
</div>
