<%= form_for(@station) do |f| %>
  <%= render 'common/errors', resource: @station %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
