<% if @tickets.empty? %>
  <p><%= t('.no_tickets')%></p>
<% else %>
  <p> <%= t('.user_tickets')%></p>
  <ul>
   <% @tickets.each do |ticket| %>   
    <li><%= link_to "#{ticket.number}", ticket_path(ticket) %></li>
   <% end %>
  </ul>
<% end %>

<%= link_to t('.profile'), current_user %>